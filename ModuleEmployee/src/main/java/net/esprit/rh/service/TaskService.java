package net.esprit.rh.service;

import java.util.List;
import java.util.Optional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import net.esprit.rh.domain.ConjeRepository;
import net.esprit.rh.domain.Employee;
import net.esprit.rh.domain.EmployeeRepository;
import net.esprit.rh.domain.conje;

@Service
public class TaskService {
	
	/*@Autowired
	private EmployeeRepository empRepository;
	
	@Autowired
	private ConjeRepository taskRepository;
	
	public void addTask(conje task, Employee emp) {
		task.setEmp(emp);
		taskRepository.save(task);
	}
	
	public List<conje>  findUserTask(Employee emp){
		
		return taskRepository.findByUser(emp);
	}*/
	
	


	
    
}
